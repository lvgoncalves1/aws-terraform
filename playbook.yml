- hosts: terraform-ansible
  tasks:
  - name: criando arquivo
    copy:
      dest: /home/ubunt/index.html
      content: <h1>terraform e ansible</h1>
  - name: criando servidor
    shell: "nohup busybox httpd -f -h /home/ubuntu -p 8080 &"
